# 오픈월드 구현과 확인 — 2026-09-10

메인 모험 화면을 Gaesup World 기반 240×240 연속 지형으로 전환했다. 플레이어의 파트너 포켓몬이 유일한 조작 캐릭터다. 원통형 트레이너를 제거하고 키보드·터치 입력, 미니맵, 카메라를 파트너의 실제 좌표에 연결했다. 수평·수직 회전과 확대·축소·시점 초기화를 제공한다.

자동 사냥은 목표 선택, 실제 접근, 자동 전투, 경험치·머니 지급, 레벨업·진화, 4~6초 리젠으로 이어진다. 실제 기술 네 슬롯과 PP·타입·명중·위력·우선도·스피드·습득 레벨을 기존 엔진 규칙으로 처리한다. 이동 속도와 표시 크기는 종족 스피드와 도감 키를 게임 단위로 변환한다. 수동 이동과 기술 지시는 자동 정책의 학습 보상에서 제외한다.

Kenney Nature Kit 2.1의 CC0 소품 12종을 내려받아 정규화하고 나무·풀·꽃·바위·길 주변에 인스턴스로 배치했다. 원본 해시와 사용 파일은 `public/models/openworld/manifest.json`, 제작 과정은 `docs/openworld-assets.md`에 있다.

## 확인한 실행 결과

- 실제 GLB 8종의 대기·걷기·공격 576프레임을 별도로 생성한 지형 raycast와 비교했다. 최대 접지 오차는 약 `1.89e-8m`였다. 애니메이션의 root scale이 표시 크기를 덮어쓰는 원인을 제거했다. 파이리의 세 포즈 최대 높이는 0.767m, 캐터피 0.762m, 뿔충이 0.713m였다.
- 브라우저에서 파이리를 선택해 80초 자동 실행했다. 327틱 동안 두 번 승리하고 레벨 5에서 8로 성장했으며 패배 후 회복과 야생 개체 3회 교체도 확인했다. 브라우저 오류는 없었다. 기록은 `artifacts/autohunt-browser-2026-09-10T14-55-33-671Z/`에 있다.
- 파트너 직접 이동, 수평·수직 카메라 조작, 일시 정지 후 동일 좌표 복원, 네 기술 슬롯, 151종 도감과 검색, 390px 모바일 화면을 검사했다. `artifacts/openworld-smoke-partner-release/`에 화면과 결과를 저장했다.
- 브라우저의 전투 승리·머니·레벨 진화·포획·개체 저장 시나리오 3개가 통과했다. 코어 회로·포켓몬 규칙·저장·오픈월드 테스트는 47/47, 프로덕션 빌드는 통과했다.
- 실제 MaleCNS 128뉴런·3,623연결의 계산 경로, 평가 가중치 고정, 개체별 기억 분리, 체크포인트 재생과 연결 절제를 확인했다. 3개 별도 시드의 각 150초 자동 사냥에서 18회 조우·13회 승리·18회 리젠을 기록했다. 자세한 조건과 제한은 `docs/openworld-neural.md`에 있다.

## 배포와 범위

GitHub 대상은 `https://github.com/jigglypop/choketmonster`의 `main`, AWS 대상은 기존 `choketmonster-web` S3·CloudFront 스택이다. 배포 스크립트는 검증한 파일 해시를 확인하고 자산 업로드 이후 HTML을 게시한다. 포켓몬 모델·스프라이트 바이너리는 이 저장소와 AWS 배포 묶음에서 제외하고 운영 앱은 고정된 외부 원본 URL에서 읽는다. CC0 자연 소품은 배포에 포함된다.

2026-09-11 KST 배포 완료: [게임 실행](https://d3b0jo8g1tseoa.cloudfront.net). 코드 커밋은 `0a43820`이며 운영 서버의 27개 파일 해시와 브라우저 플레이 검사를 통과했다. 상세 기록은 `docs/deployment.md`에 있다.

감각 입력·동역학·학습 규칙·공격 보정·이동 속도는 게임을 위해 설계한 부분이다. 실행 결과는 생물학적 학습이나 포켓몬 지능을 입증하지 않는다. 전투는 지원한 효과 범위의 축약 규칙이며 외부 모델 로딩에는 인터넷 연결이 필요하다. 빌드는 통과했지만 3D 실행 청크의 크기 경고가 남아 있다.
