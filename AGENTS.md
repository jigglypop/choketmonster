# 초켓몬스터 작업 지침

사용자의 최신 요청을 최우선으로 따른다. 스킬이나 이 문서가 최신 요청과 충돌하면 최신 요청을 적용하고, 충돌한 문구를 함께 고친다.

## 목표

초파리 커넥톰에서 가져온 회로가 게임 속 개체의 행동에 실제로 관여하는 실행 가능한 게임을 만든다. 공개 원본의 포켓몬 자료를 지원하고 각 개체의 뇌 상태와 학습 기록을 따로 저장한다. 성도와 관동은 지역별 출현 종이 가장 다양한 버전을 원본 자료로 비교해 하나씩 고정하며, 수집 버전 선택 UI는 제공하지 않는다. 런타임 출현은 원본의 아침·낮·밤 분포를 합쳐 시간 제한 없이 제공한다. 원본 시간별 자료와 기존 저장의 시간 값은 보존하고, 원본에서 빠지는 종은 희귀 추가 분포로 얻을 수 있게 한다. 원본 분포와 자체 추가 분포를 구분해 기록한다. 기존 버전별 수집 데이터는 저장 호환성을 위해 보존한다. 중복 개체 경험치 합치기는 사용자가 남길 개체를 선택하며 회로 기억은 합치지 않는다.

추가 지역은 공개 지도 정보의 도시·도로 연결을 근거로 관동처럼 자체 3D 지형을 재구성할 수 있다. 완성된 외부 GLB 맵 파일만을 요구하지 않는다. 실제 3D 지형, 통행·충돌, 지역별 고정 야생 분포와 사용할 포켓몬 모델·리깅 동작을 실행 검증한 범위만 플레이 선택지에 넣는다. 미검증 지역은 선택지에서 제거하되 기존 저장과 개체 기록은 보존한다. 포켓몬 3D 확보는 내려받아 검증한 기하 모델을 기준으로 하며, 이미지나 임시 도형을 해당 포켓몬 모델로 세지 않는다.

원본에 골격이나 동작이 없다는 이유로 성도 출현 종을 제한하지 않는다. 확보한 기하 모델에 몸 형태에 맞는 골격·스킨 가중치·동작을 직접 추가하고 실행 검증한다. 원본과 작업본은 구분해 보존하며, 자체 제작한 리깅·동작을 원본 애니메이션이라고 표시하지 않는다.

커넥톰은 신경 연결 지도다. 그 자체를 완성된 뇌나 포켓몬 지능으로 설명하지 않는다. 감각 입력, 신경 동역학, 행동 출력, 학습 규칙 중 직접 설계한 부분은 화면과 결과 문서에 표시한다.

## 진행 권한

요청받은 기능을 완성하는 데 필요한 저장소 조사, 코드 수정, 공개 자료 확인과 다운로드, 의존성 설치, 로컬 데이터 변환, 제한된 실험, 브라우저 검사, 실패 수정은 묻지 말고 계속 진행한다. 첫 구현에서 멈추지 말고 실행 결과를 확인하고 필요한 수정까지 마친다.

유료 API·유료 계산 작업, 계정 로그인, 외부 게시, 배포, 라이선스 조건이 불명확한 자산의 재배포, 사용자 데이터 삭제처럼 결과가 큰 행동은 요청에서 이미 승인한 범위를 확인한다. 이번 요청의 기존 AWS CLI를 이용한 저비용 배포는 승인된 범위다. 그 밖에 새로 필요한 큰 행동만 직전에 사용자 승인을 받는다. 그 전까지 가능한 로컬 구현과 검증을 모두 끝내 검토 가능한 상태로 만든다.

실제 데이터 작업은 원본을 수정하지 않고 새 폴더에 저장한다. 오래 걸리거나 큰 다운로드는 재개할 수 있게 부분 파일과 체크섬을 사용한다. 실패 시 자동으로 같은 유료·대용량 작업을 반복하지 않는다.

## 반드시 지킬 것
- 자동 모드는 가까운 야생 포켓몬을 선택·추적하고 접근하면 배틀한다. 별도 자동 사냥 스위치는 두지 않는다.
- 마을 지상 구역에는 야생 포켓몬이 출현하거나 들어오지 못하며 새 야생 전투를 시작하지 않는다. 수동 이동 입력이나 클릭 이동 경로가 끝나면 즉시 자동으로 돌아간다.
- 기술 사용 횟수는 전투 안팎에서 무제한이며 PP와 자동 회복 문구를 화면에 표시하지 않는다. 기존 PP 숫자는 저장 호환성용으로만 보존한다.
- 동굴 내부는 미로 없이 통행 가능하게 하고 바닥과 외곽 벽에 돌 텍스처를 사용한다. 입출구와 지역별 출현표는 유지한다.
- 사람형 필드 NPC는 지상·동굴에서 모두 제외한다. 과거 트레이너 대전·진행 기록과 원본 데이터는 저장 호환성을 위해 보존한다.
- 밤낮에 따른 조명·하늘색 전환과 시계 표시는 제거하고 지상은 항상 낮 밝기를 사용한다. 원본 출현 자료와 기존 저장의 시간 값은 보존한다.
- 안개는 제거한다. 그림자와 수면 효과는 유지하되 가까운 영역만 상세하게 표시하고, 모바일에서는 범위·해상도·갱신 빈도를 낮춘다. 지형·포켓몬과 이동 반응성을 우선한다.
- 지형의 풀·흙 텍스처를 유지한다. 성능 개선은 공유 재질·버퍼 재사용과 LOD로 수행하며 지형을 단색으로 바꾸지 않는다. 기본 이동 속도는 종족 속도식의 2.4배를 기준으로 유지하고, 사용자의 요청 없이 다시 크게 높이지 않는다.

- 브라우저 기본 `alert`, `confirm`, `prompt`는 사용하지 않는다. 합치기·놓아주기 등 사용자 확인은 앱 모달로 제공하고, 처리 결과는 화면 안에 표시한다.
- 합성 회로는 `synthetic`, 실제 데이터에서 만든 회로는 `connectome-subset` 또는 실제 범위에 맞는 이름을 사용한다.
- 실제 회로에는 원본 URL, 데이터셋과 버전, 라이선스, 원본 파일 SHA-256, 선택·제외 기준, 변환 가정을 남긴다.
- 64비트 뉴런 ID는 문자열로 보존한다.
- 렌더링은 시뮬레이션 난수를 소비하지 않는다. 개체마다 상태를 따로 소유한다. 평가 중에는 학습 가중치를 바꾸지 않는다.
- 보상 증가, 움직이는 화면, 테스트 통과를 생물학적 학습의 증거로 표현하지 않는다. 무작위·학습 전 정책과 같은 평가 조건에서 비교한다.
- 포켓몬 이름·도감·이미지의 출처와 사용 조건을 기록한다. 로컬 개발을 넘어 배포할 때는 권리 범위를 다시 확인한다.

## 필요한 문서만 읽기

- 신경망·정책·평가 변경: `docs/experiments.md`
- 실제 커넥톰 가져오기: `docs/connectome.md`
- 포켓몬 데이터·버전별 수집·화면 작업: 해당 코드와 자산 출처 문서
- 작업을 나눠 병렬로 진행할 때: `docs/model-routing.md`
- 단순 문구·스타일 변경: 수정할 파일만 확인

문서 전체나 저장소 전체를 매번 읽지 않는다. 현재 작업에 필요한 파일부터 찾고, 막히거나 범위가 넓어질 때만 관련 문서를 추가로 연다.

## 모델 혼합

기본 책임자는 GPT-6 Astra다. 설계 판단, 작업 분해, 결과 통합, 실제 커넥톰의 근거 판단과 최종 완료 판정은 Astra가 맡는다. 경계가 분명한 구현·테스트·데이터 정리 작업은 GPT-5.6 Sol의 `medium` 추론 강도로 위임할 수 있다.

작은 수정이나 서로 의존하는 작업을 억지로 나누지 않는다. 병렬화가 실제로 시간을 줄이거나 독립 검토가 결과를 개선할 때만 위임한다. Astra는 위임 결과를 그대로 전달하지 않고 변경 내용과 검증 근거를 확인해 전체 목표에 합친다.

## 완료와 검증

완료는 구현, 실제 실행, 결과 확인, 발견한 실패 수정까지 포함한다. 코드 검사와 제품 성공을 분리해 보고한다.

- 문구·CSS처럼 되돌리기 쉬운 변경은 해당 화면이나 빌드만 확인한다. 구현을 그대로 따라 하는 테스트를 추가하지 않는다.
- 코어 동작 변경은 관련 단위 테스트와 `npm run build`를 실행한다.
- 사용자 흐름 변경은 해당 브라우저 시나리오만 검사한다.
- 학습·보상·회로 변경은 관련 테스트, 제한된 비교 실험, 저장된 리플레이를 확인한다.
- 실제 데이터 가져오기는 체크섬, 변환 통계, 회로가 정책 실행에 사용됐다는 증거를 확인한다.
- 프론트엔드는 터미널의 pnpm dev로 실행하고, 백그라운드로 절대 실행하지 않는다.

관련 검사가 통과하면 새 실패나 미확인 위험이 없는 한 전체 검사를 반복하지 않는다. 범위 밖 작업이 남으면 구현된 결과와 다음 입력을 구체적으로 기록한다.
